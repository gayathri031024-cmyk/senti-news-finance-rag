# Used by docker-compose.yml to configure the Postgres container.
# Copy to .env and adjust if you don't want the defaults.
# Never commit the real .env file.

POSTGRES_USER=sentinews
POSTGRES_PASSWORD=sentinews
POSTGRES_DB=sentinews
